<h1>Burgers!</h1>

<h2>Burgers you have eaten!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers you haven't eaten!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> cburgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger!</h2>
<form class="create-form">

  <div class="form-group">
    <label for="add">Burger name:</label>
    <input type="text" id="add" name="name">
  </div>

  <button type="submit">Add Burger</button>
</form>